{
  "name": "kinesisreader",
  "version": "1.0.0",
  "description": "Lambda function which gets triggered when the kinesis stream generates the file on S3 bucket. Once this function gets triggered, it invokes the shell script on the EMR cluster.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.370.0",
    "fs": "0.0.1-security",
    "simple-ssh": "^1.0.0"
  }
}
